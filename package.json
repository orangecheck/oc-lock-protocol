{
  "name": "oc-lock",
  "version": "2.0.0",
  "private": true,
  "description": "OC Lock protocol — Bitcoin-identity-bound end-to-end encryption.",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "build": "yarn workspace @oc-lock/crypto build && yarn workspace @oc-lock/core build && yarn workspace @oc-lock/device build",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vitest": "^3.2.4"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/orangecheck/oc-lock.git"
  },
  "license": "MIT"
}
